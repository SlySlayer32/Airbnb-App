# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
# Environment files should be ignored to prevent credential exposure
!.env
.env.local
!.env.example

# Node.js dependencies - too large and not needed for AI context
node_modules/

# Git repository data
.git/

# Expo build cache and temporary files
.expo/
.expo-shared/

# VS Code settings and cache
!.vscode/

# Metro bundler cache
.metro/
.metro-cache-*/

# Build outputs
dist/
build/

# OS generated files
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Package lock files (too large for indexing)
package-lock.json
yarn.lock

# Temporary files
*.tmp
*.temp

# IDE files
.idea/
*.swp
*.swo
